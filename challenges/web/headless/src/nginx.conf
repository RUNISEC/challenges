server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    add_header X-Server-Flag "RUNISEC{h34d3rs_sp34k_l0ud3r}" always;

    location / {
        try_files $uri $uri/ =404;
    }
}
